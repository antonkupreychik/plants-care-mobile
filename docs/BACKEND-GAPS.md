# Backend gaps — найдено при разработке мобилки

> Живой лог: что мобильному клиенту нужно от API, но в контракте этого нет или
> оно неудобно. Заполняется по ходу фич. Бэкенд дорабатывается отдельно
> (владелец), мобилка пока ставит заглушку и помечает `TODO(gap #N)`.
>
> Это дополняет `design_handoff_plantcare/api-contract.md` §12 (исходный список
> от дизайна) — здесь фиксируем то, что реально упёрлись при имплементации.
>
> Статусы: 🔴 нет в API · 🟡 есть, но неудобно/неполно · 🟢 закрыто бэкендом.

## Легенда полей
`ID` — локальный номер · `Экран` · `Нужно` · `Сейчас в API` · `Предложение` · `Статус`

---

## Сводка сверки с live-бэкендом — 2026-05-28

> Прокурлено по `plants-care-development.up.railway.app` + сверено с api-docs и
> *статической* спекой `api/openapi/` (источник кодгена). «Бэк отдаёт» ≠ «мобилка
> готова»: если эндпоинта нет в `api/openapi/`, клиент не сгенерён — мобилке сначала
> добавить в спеку и регенерить.

**Закрыто бэкендом (зашиплено в мобилку):** G1, G6, G9, G10, G13a.

**Бэк уже отдаёт — мобилке остаётся подключить (в `api/openapi/` ещё нет):**
- **G4** `GET /weather/snapshot` → `{available, humidityPercent, recommendation DEFER_OK|DO_NOT_DEFER|NEUTRAL, fetchedAt, fromCache}`.

**Бэкенду ещё нужно сделать (backend-задачи, мобилке делать нечего):**
- **G2** — `status`/`moodLabel`/`voiceLine` нигде нет.
- **G3** — явного флага «проблемное растение» нет; теперь **выводимо клиентом из G1** (`zone == RED`) — решить, нужен ли отдельный сигнал.
- **G5** — `/me` → `404`; счётчиков/имени/аватара нет.
- **G11** — `TaskDto` в `/calendar` без `doneAt`/`status` → прогресс `done/count` (экран 11) нельзя.
- **G13b** — `/today` = `{tasks,count}`, фида/счётчика выполненного нет → прогресс-кольцо (экран 03) нельзя.
- **G14** — эндпоинтов расписаний нет, `POST /plants` интервалы не принимает (speciesId связывает вид, но расписания не создаёт).
- **G12** — эндпоинт `GET /calendar/{token}.ics` **появился**, но **источника `token` в API нет** (`/me` 404) → подписку не собрать. Нужен способ получить персональный token.
- **G15** — в `PlantDto` появился флаг `archived`, но memorial-полей (`archivedAt`/причина/`livedFor`/`gifted`) и списка архивных нет.

**Новые эндпоинты, появившиеся к этой сверке (для сведения):**
`GET /care-types` (display-имена тип-задач), `GET /stats/streak?plantId=` → `{plantId, streak}`,
`GET /plants/{id}/health` (G1), `GET /weather/snapshot` (G4), `GET /calendar/{token}.ics` (G12),
`POST /auth/{apple,google,email/request,email/verify,refresh}` (фаза auth).

> ⚠️ `/care-types` отдаёт только локализованные имена (`WATERING→«Полив»`) — enum-разрыв
> **G7** (`taskType` WATERING ↔ care-event `type` WATER/SPRAY) НЕ закрывает.

### Backend-тикеты (`antonkupreychik/plants-care`)
Заведено 2026-05-28 по открытым гэпам:

| Gap | Issue | Gap | Issue |
|---|---|---|---|
| G11 + G13b | [#179](https://github.com/antonkupreychik/plants-care/issues/179) | G22 | [#191](https://github.com/antonkupreychik/plants-care/issues/191) |
| G16 (+G5) | [#182](https://github.com/antonkupreychik/plants-care/issues/182) | G23 | [#192](https://github.com/antonkupreychik/plants-care/issues/192) |
| G17 | [#183](https://github.com/antonkupreychik/plants-care/issues/183) | G24 | [#193](https://github.com/antonkupreychik/plants-care/issues/193) |
| G19 (+G14) | [#185](https://github.com/antonkupreychik/plants-care/issues/185) | G25 | [#194](https://github.com/antonkupreychik/plants-care/issues/194) |
| G28 | [#186](https://github.com/antonkupreychik/plants-care/issues/186) | G26 | [#195](https://github.com/antonkupreychik/plants-care/issues/195) |
| G18 | [#187](https://github.com/antonkupreychik/plants-care/issues/187) | G27 | [#196](https://github.com/antonkupreychik/plants-care/issues/196) |
| G20 | [#188](https://github.com/antonkupreychik/plants-care/issues/188) | G21 | [#189](https://github.com/antonkupreychik/plants-care/issues/189) |

> Без тикета (намеренно): G2 (решение voiceLine клиент/бэк), G3 (выводим из G1),
> G7 (дешёвый клиентский маппинг), G8 (тулинг), G12 (ждёт источник token),
> G15 (#117, отдельный roadmap-трек архива).

---

## G1 · Health Score растения 🟢
- **Экран:** 01 Home (мини-кольцо на карточке), 02 Plant card (бейдж).
- **Закрыто (2026-05-28):** бэкенд отдаёт `GET /api/v1/plants/{id}/health` →
  `{insufficientData: bool, score: int 0–100|null, zone: GREEN|YELLOW|RED|null}`.
  Публичный (200 без auth-заголовков → клиент шлёт `AuthScope.none`). При
  `insufficientData=true` (< 3 записей ухода) `score`/`zone` = **`null`** (в спеке поля
  nullable, required только `insufficientData`). Эндпоинт ДОБАВЛЕН в статическую спеку
  (`resources/plant-health.yaml`), клиент перегенерён (MADR-007). Мобилка рисует кольцо
  (Home) и бейдж (Plant card); `insufficientData`/`null` → нейтральное «—», не как ошибка.
- **Примечание:** реальная схема `{insufficientData, score, zone}` РАСХОДИТСЯ с прогнозом
  api-contract §12.4 (`{score, factors[], recommendation?}`) — стоит поправить §12.4.
- **Follow-up:** см. **G16** — для кольца на Home это per-plant запрос (N+1).

## G2 · Mood / voice line растения 🔴
- **Экран:** 01 Home (подпись-настроение), 02 Plant card (speech bubble).
- **Нужно:** `status` (`happy|thirsty|sick`) и/или текст реплики от первого лица.
- **Сейчас:** нет. Бренд-фича, текст пока генерится на клиенте.
- **Предложение:** `status`/`moodLabel` в `PlantDto`, `voiceLine` в `TaskDto`/`PlantDto`
  (api-contract §12.3). Решить: клиент или бэк генерит реплики.
- **Заглушка мобилки:** генерим на клиенте по `taskType` + просрочке.

## G3 · Алерт «проблемное растение» на Home 🔴
- **Экран:** 01 Home (баннер сверху).
- **Нужно:** признак, что у растения проблема (для баннера) + ссылка на растение.
- **Сейчас:** нет.
- **Предложение:** флаг в `/today` или `/me` (`attentionPlantId?`), либо вывести из G1 (health < порога).
- **Сверка 2026-05-28:** явного флага по-прежнему нет, но G1 закрыт — баннер теперь
  **выводим клиентом** из `health.zone == RED` (ценой N запросов /health). Решить: ждать
  явный `attentionPlantId` от бэка или собирать из G1.
- **Заглушка:** баннер не показываем, пока нет сигнала.

## G4 · Виджет погоды на Home 🟢
- **Экран:** 01 Home (микро-строка погоды).
- **Закрыто (2026-05-29):** бэк отдаёт `GET /api/v1/weather/snapshot` →
  `{available: bool, humidityPercent: int 0–100|null, recommendation: DEFER_OK|DO_NOT_DEFER|NEUTRAL|null,
  fetchedAt: date-time|null, fromCache: bool|null}` (проверено curl, `200`; публичный —
  клиент шлёт `AuthScope.none`). При `available=false` остальные поля **`null`** (погода не
  настроена / источник недоступен) — в спеке `required: [available]`, остальное nullable.
  Эндпоинт ДОБАВЛЕН в статическую спеку (`resources/weather.yaml`), клиент перегенерён
  (MADR-007). Мобилка рисует микро-строку: влажность + совет из `recommendation`; при
  `!available`/loading/error строку тихо скрываем.
- **Примечание:** реальная схема расходится с прогнозом api-contract §12.8
  (`{temp, humidity, advice}`) — температуры и текста погоды НЕТ, только влажность +
  recommendation-enum. Стоит поправить §12.8.

## G5 · Счётчики пользователя (header Home) 🔴
- **Экран:** 01 Home (приветствие, badge уведомлений).
- **Нужно:** `{plantsTotal, tasksToday, notificationsUnread}` + имя/аватар.
- **Сейчас:** нет `/me`. api-contract §12.1.
- **Заглушка:** `tasksToday` берём из длины `/today`; имя — заглушка; badge не рисуем.

## G6 · Привязка задачи/растения к виду (иллюстрация) 🟢
- **Экран:** 01 Home, 03 Today — нужно выбрать SVG-иллюстрацию (Monstera/Fern/...).
- **Нужно:** по `TaskDto`/`PlantDto` понять вид, чтобы выбрать иллюстрацию.
- **Было:** `PlantDto.speciesId/speciesName` есть; `TaskDto` (в /today,/calendar) — только
  `plantName`, без вида. На Today-задаче не из чего было выбрать иллюстрацию.
- **Закрыто (2026-05-28):** бэкенд добавил в `TaskDto` поля `speciesId` (int64, nullable)
  и `speciesName` (string, nullable) в `/today` и `/calendar`. Проверено curl. Мобилка:
  спека `calendar.yaml` обновлена, клиент регенерён, поля проброшены в domain `CareTask`
  и mapper; карточки задач Today (`today_card`, `today_task_card`) рисуют
  `PlantIllustration` по виду вместо нейтрального глифа. При отсутствии вида (`null`) —
  дефолтная иллюстрация через `PlantArt.fromSpecies`.

## G7 · `taskType` (today/calendar) ≠ `type` (care-events) 🟡
- **Везде, где выполняем уход.**
- `/today`,`/calendar` отдают `WATERING/MISTING/FERTILIZING/SOIL_CHECK`, а `POST /care-events`
  принимает `WATER/SPRAY/FERTILIZE`. Маппинг делаем на клиенте (domain-слой).
- **Предложение бэку:** согласовать enum’ы или отдавать оба. Низкий приоритет (маппинг дешёвый).

## G8 · Рантайм `/v3/api-docs` не документирует заголовки/параметры 🟡
- **Тулинг (кодген).** springdoc на dev-хосте не видит `X-User-Id`/`X-Chat-Id` и query-параметры
  (`locationId/offset/limit`) — PoC резолвит заголовки через `UserApiResolver` мимо аннотаций.
  `/calendar` отдаёт пустую схему (springdoc не сериализовал `Map<String,List<TaskDto>>`).
- **Следствие:** кодген идёт из статической `api/openapi/`, а не из рантайма (см. memory
  `openapi-codegen-source`).
- **Предложение бэку:** добавить `@Parameter` для заголовков/квери и именованную
  `CalendarResponse`-обёртку, чтобы `/v3/api-docs` стал пригоден как источник.

## G9 · `GET /plants` отдаёт 500 для user 1 (dev) 🟢
- **Экран:** 01 Home (сетка растений).
- **Симптом (был):** `GET /api/v1/plants` с `X-User-Id: 1` на dev-хосте → `500 INTERNAL_ERROR`
  (проверено curl 2026-05-27). `/locations` (200) и `/today` (404) для тех же id отвечают.
- **Следствие (было):** на Home секция растений уходит в ErrorState. Клиент обрабатывает
  корректно (это не баг мобилки), но протестировать «живой» сад на dev было нельзя.
- **Закрыто (2026-05-27):** бэкенд починен. `GET /api/v1/plants` с `X-User-Id: 1` теперь
  отдаёт `200` с реальными данными (растения user 1, напр. «Монстера» id 7, 25). Проверено
  curl с/без `Accept`, а также с `Accept: application/json` — все варианты `200`. На Home
  можно тестировать «живой» сад. (`/today` для user 1 всё ещё `404` — отдельный вопрос.)

## G10 · `GET /plants/{id}/history` отдаёт 500 (dev) 🟢
- **Экран:** 02 Карточка растения (лента истории ухода).
- **Симптом (был):** `GET /api/v1/plants/{id}/history` с валидным `X-Chat-Id: 764602851`
  → `500 INTERNAL_ERROR`. Воспроизводилось для всех растений и любых параметров.
  Проверено curl 2026-05-27.
- **Следствие (было):** секция «История ухода» на экране 02 уходила в ErrorState
  (деталь и стрик на той же карточке работали).
- **Закрыто (2026-05-27):** бэкенд починен. `GET /plants/{id}/history` теперь отдаёт
  `200` с реальными событиями (`items[]` с `type` WATER/SPRAY, `performedAt`, `onTime`).
  Форма совпадает с маппером `care_history_mapper`. Секция истории на экране 02
  наполняется.

## G11 · `/calendar` без признака выполнения задач 🔴
- **Экран:** 11 График (недельный календарь).
- **Нужно:** по задаче в `/calendar` понимать, выполнена ли она — для прогресса дня
  `done/count` (полоса в дизайне экрана 11) и состояния «готово»/«просрочено».
- **Сейчас:** `GET /api/v1/calendar` отдаёт только **запланированные** задачи (`TaskDto`:
  `scheduleId/plantId/plantName/taskType/nextDueAt`). Признака выполнения нет — `done`
  посчитать нельзя. Та же `TaskDto`, что в `/today`.
- **Предложение:** добавить в `TaskDto` (для calendar) флаг/время выполнения, например
  `doneAt: string?` (или `status: PENDING|DONE`), либо отдельную карту выполненных
  `care-events` на диапазон, чтобы клиент сматчил по `scheduleId`+дате.
- **Заглушка мобилки:** прогресс-бар `done/count` из дизайна **не рисуем**; в строке дня
  показываем счётчик задач (`scheduleDayTasksCount`) или «Свободно». «Просрочено»
  выводим клиентом по `nextDueAt < now` (раскрытый сегодняшний день). «Готово» нигде.

## G12 · Экспорт календаря `.ics` отсутствует 🟡
- **Экран:** 11 График (карточка «Подписаться в календаре»).
- **Нужно:** подписка в Google/Apple Calendar (api-contract §11 «Хотелки», #79).
- **Сверка 2026-05-28:** эндпоинт **появился** — `GET /calendar/{token}.ics` (token в пути,
  отдаёт VCALENDAR-строку). **Но:** способа получить персональный `token` в API нет —
  `/me` отвечает `404`, отдельного эндпоинта подписки/токена нет. Без token URL не собрать.
- **Осталось бэкенду:** отдать пользователю его `token` (поле в будущем `/me`, либо
  `GET /calendar/subscription` → `{url|token}`). Плюс эндпоинт не в статической `api/openapi/`.
- **Заглушка мобилки:** карточка `.ics` (`ScheduleIcsCard`) отрисована, но **инертна** —
  тап → snackbar «Скоро» (`comingSoon`). Включим, когда появится способ получить token.

> **Клиентская ловушка (не backend-гэп), экран 11.** Спека объявляет query `from`/`to`
> у `/calendar` как `type: string, format: date` (`YYYY-MM-DD`) — это **корректно**, бэкенд
> на `YYYY-MM-DD` отвечает `200`. Но `swagger_parser`/`retrofit` генерят их как Dart
> `DateTime` и сериализуют через `toIso8601String()` → уходит `2026-05-20T00:00:00.000`,
> на что бэк отдаёт `500`. Воркэраунд — рантайм-интерсептор `DateQueryInterceptor`
> (`core/network`, opt-in через `dateOnlyQueryExtra({'from','to'})`), усекающий значение
> до даты. Сгенерированный код НЕ правился. Снять, когда кодген начнёт сериализовать
> `format: date` как date-only.

## G13a · `POST /plants` не принимает `speciesId` 🟢
- **Экран:** 04 Мастер добавления (шаг 1 «Выбор вида»).
- **Нужно:** связать создаваемое растение с выбранным видом (для иллюстрации/плана ухода
  на Home/Card — ср. G6) — т.е. `speciesId` в теле создания.
- **Было:** `PlantCreateRequest` = `{ name (1..100), notes? (≤2000), locationId? }`. Поля
  `speciesId` не было, хотя `GET /plants` его **возвращает** (`PlantDto.speciesId`) —
  асимметрия контракта: связь нельзя было задать при создании.
- **Закрыто (2026-05-28):** бэкенд добавил опциональный `speciesId: int64` в
  `PlantCreateRequest` (вид должен существовать, иначе `404`; **расписания при этом НЕ
  создаются** — отдельный gap G14). Мобилка: спека `plants.yaml` обновлена, клиент
  регенерён, `AddPlantRepository.createPlant` принимает `speciesId`, мастер шлёт
  `draft.species?.id` (null если вид не выбран). Связь растение↔вид теперь фиксируется.
- **Осталось (G14):** дефолтные расписания из вида бэкенд по-прежнему не создаёт —
  шаг 3 мастера остаётся read-only превью.

## G14 · Расписания не задаются при создании растения 🔴
- **Экран:** 04 Мастер добавления (шаг 3 «Расписание ухода»).
- **Нужно:** задать/подтвердить интервалы ухода для нового растения (дизайн шага 3 —
  редактируемые расписания).
- **Сейчас:** расписания наружу не выставлены (api-contract §12.2: ни
  `GET/PUT /plants/{id}/schedules`, ни `GET /species/{id}/care-defaults`), а
  `POST /plants` интервалы не принимает. Дефолты приходится считать из
  `wateringDays/mistingDays/fertilizingDays/soilCheckDays` вида на клиенте.
- **Предложение:** §12.2 — эндпоинты расписаний + (опц.) приём интервалов в создании
  или авто-создание расписаний из `speciesId` (см. G13a).
- **Заглушка мобилки:** шаг 3 — **read-only** превью плана из интервалов вида
  (`SpeciesSummary.carePlan`), пометка «Только просмотр»; ничего не редактируется и не
  персистится. Реальные расписания появятся, когда откроют §12.2.

---

## G13b · Нет фида «выполнено сегодня» / счётчика done 🔴
- **Экран:** 03 Сегодня (полный список задач ухода).
- **Родственный:** G11 (тот же дефицит «признака выполнения», но для `/calendar`/экрана 11).
- **Нужно:** для прогресса дня — сколько задач уже выполнено сегодня и какие
  (имя растения, тип, время отметки). Дизайн `screens-v2.jsx` (TodayScreen)
  показывает прогресс-кольцо «X из N выполнено», подпись «Осталось Y · Z просрочена»
  и свёрнутую секцию «N выполнено сегодня · растение · полит в 7:42».
- **Сейчас:** `GET /today` отдаёт только **pending**-задачи (активные расписания,
  дедлайн ≤ конца дня в TZ пользователя). Выполненные сегодня не возвращаются,
  отдельного эндпоинта/счётчика done на сегодня нет. `/plants/{id}/history` — по
  одному растению, не агрегат за день по всем растениям.
- **Предложение:** либо расширить `/today` полем `doneToday` (count + краткий
  список выполненных за сегодня в TZ пользователя), либо отдельный
  `GET /today/completed`. Минимум — счётчик done, чтобы посчитать «X из N».
- **Заглушка мобилки:** прогресс-кольцо и секцию «выполнено сегодня» не рисуем.
  Summary показывает только `totalCount` (pending) + `overdueCount` (вычисляется
  на клиенте: `dueAt.toLocal() < now`). Overdue считается клиентом, а не приходит
  с бэкенда. `TODO(gap #13)` — вернуть прогресс дня, когда появится фид done.

---

## G15 · Архив / memorial растений 🔴
- **Экран:** 17 Архив (`screens-v4.jsx → ArchiveScreen`), roadmap #117.
- **Нужно:** список архивных (выбывших) растений с memorial-данными для карточки:
  имя, вид, как долго прожило рядом, причина/заметка выбытия, дата архивации,
  флаг «подарили» vs «погибло»; плюс агрегат для блока «Ретроспектива» (средний
  срок жизни растений у пользователя).
- **Сейчас:** эндпоинта нет. `GET /plants` отдаёт только активные; ни
  `?status=archived`, ни `/archive`, ни полей архивации в `PlantDto` (дата выбытия,
  причина, длительность) не существует. Среднего срока тоже нет.
- **Сверка 2026-05-28:** в `PlantDto` появился булев флаг `archived` — но это всё:
  ни списка архивных (фильтра `?status=archived`), ни memorial-полей
  (`archivedAt`/причина/`livedFor`/`gifted`), ни ретроспективы по-прежнему нет.
- **Предложение:** `GET /plants?status=archived` (или `/archive`) → растения с
  полями `archivedAt`, `cause/note`, `livedFor` (или вычислять из created/archived),
  `gifted: bool`; ретроспектива — отдельным полем/эндпоинтом либо считать на клиенте,
  когда появятся даты. Для архивации — `PATCH /plants/{id}` со `status`.
- **Заглушка мобилки:** экран собран **UI-first** — domain-интерфейс
  `ArchiveRepository` + `FakeArchiveRepositoryImpl` (хардкод 3 растения + ретроспектива
  из дизайна). Реальная dio/codegen-реализация подключится подменой
  `archiveRepositoryProvider` (scope `user`), domain/state/UI не меняются.
  `TODO(BACKEND-GAPS #117)` в фейке. Чипы «Открыть дневник»/«Вспомнить» — coming-soon.

---

> **Гэпы G16–G28 — из планирования экранов v5–v10 (дизайн от 2026-05-28).**
> Найдены при сверке новых экранов (14–38) с live-API. Привязка экранов и порядок
> реализации — в `docs/SCREENS-PLAN.md`. Roadmap-номера (`#NN`) — из
> `design_handoff_plantcare/README.md` §4.

## G16 · Настройки пользователя `/me` + `PATCH /me` 🔴
- **Экран:** 23 Тихие часы, 37 Таймзона, 38 Язык (синк), 13 Профиль (статистика/блоки).
- **Нужно:** чтение и запись персональных настроек: `quietHoursStart`/`quietHoursEnd`,
  `timezone`, `locale`; плюс read-профиль (имя/аватар/счётчики — ср. **G5**).
- **Сейчас:** `/me` → `404`. Ни чтения профиля, ни записи настроек нет.
- **Предложение:** `GET /api/v1/me` → `{name, avatar?, plantsTotal, tasksToday,
  notificationsUnread, quietHoursStart, quietHoursEnd, timezone, locale}` +
  `PATCH /api/v1/me` `{quietHoursStart?, quietHoursEnd?, timezone?, locale?}` (#116).
  Объединяет **G5** (счётчики, read) и запись настроек.
- **Заглушка мобилки:** экраны 23/37 пока не делаем; 38 Язык — локально через
  `AppLocalizations` (MADR-012), синк в `/me` подключим позже.

## G17 · Лента уведомлений 🔴
- **Экран:** 24 Лента уведомлений, 32 Пустая лента; badge 🔔 на Home 01.
- **Нужно:** список уведомлений (care/alert/award/report/system) с прочитанностью +
  отметка прочтения. Тексты «голосом растения» — ср. **G2** (voiceLine).
- **Сейчас:** эндпоинтов нет.
- **Предложение:** `GET /api/v1/notifications` → `{items[{id, type, title, body,
  plantId?, createdAt, readAt?}], unreadCount}` + `POST /api/v1/notifications/{id}/read`
  (api-contract §12.9). Badge на Home берётся из `unreadCount` (закрывает часть G5).
- **Заглушка мобилки:** экран и badge не делаем, пока нет фида.

## G18 · Регистрация push-устройства 🔴
- **Экран:** 27 Онбординг разрешения на пуши.
- **Нужно:** отдать серверу push-токен устройства после `requestPermissions`.
- **Сейчас:** эндпоинта нет. + **Открытый вопрос** (README §9 #3): Expo Push vs FCM/APNs —
  решение влияет на форму токена и весь push-стек.
- **Предложение:** `POST /api/v1/devices` `{pushToken, platform: ios|android}` (+ опц.
  `DELETE /api/v1/devices/{token}` при логауте).
- **Заглушка мобилки:** экран прайминга можно собрать UI-first, но без бэка и решения
  по стеку реальная регистрация не работает.

## G19 · Чтение/редактирование расписаний ухода 🟢
- **Экран:** 22 Редактирование расписания (4 типа: enabled, интервал, объём мл для полива).
- **Родственный:** **G14** (расписания при создании) — тот же дефицит, но G14 про создание.
- **Закрыто (2026-05-30):** бэк отдаёт `GET /api/v1/plants/{id}/schedules` →
  `[CareScheduleDto {type, every, unit (DAY), amountMl?, enabled, nextDueAt}]` и
  `PUT /api/v1/plants/{id}/schedules/{type}` `CareScheduleUpdateRequest {every, unit,
  amountMl?, enabled}` → обновлённый `CareScheduleDto` (проверено curl, `200`, user-scoped).
  Эндпоинты ДОБАВЛЕНЫ в статическую спеку (`resources/plant-schedules.yaml`), клиент
  перегенерён (MADR-007). Мобилка: фича `lib/features/edit_schedule/`, экран 22 (вход —
  карточка 02 → «Расписание ухода · Изменить»). Локальный драфт + сохранение только
  изменённых типов на «Готово» (PUT per type; partial-failure: успешные фиксируются,
  упавший остаётся грязным). «Сбросить к рекомендованным» — интервалы из вида по `speciesId`.
- **Осталось (доводки, не блокеры):**
  - **G14 / шаг 3 мастера** — расписания теперь существуют (read/edit), но `POST /plants`
    их не создаёт автоматически из вида; шаг 3 пока read-only превью. Можно после создания
    открывать экран 22 для правки, либо ждать авто-создание из `speciesId`.
  - **Экран 33** — счётчик «следующий уход через N дней» теперь ВЫВОДИМ из `nextDueAt`
    расписания (этот гэп его и блокировал); можно оживить отдельной доводкой.

## G20 · Сезонные коэффициенты к расписанию 🔴
- **Экран:** 35 Сезонные интервалы (авто-подстройка частоты полива по сезонам).
- **Нужно:** сезонные множители/интервалы к расписанию + тумблер авто-подстройки.
- **Сейчас:** нет (#67). Расписания и так не выставлены (ср. G19).
- **Предложение:** часть модели расписаний — сезонные коэффициенты на уровне
  растения/вида + флаг авто-подстройки в настройках. Зависит от G19.
- **Заглушка мобилки:** экран не делаем.

## G21 · Режим отпуска 🔴
- **Экран:** 25 Режим отпуска (диапазон дат, пауза пушей, передача догляда).
- **Сейчас:** нет (#53).
- **Предложение:** `POST /api/v1/vacation` `{from, to}`, `DELETE /api/v1/vacation`;
  на период — пауза напоминаний (и, опц., перенос дедлайнов).
- **Заглушка мобилки:** экран не делаем.

## G22 · Совместный уход (sharing) 🔴
- **Экран:** 26 Совместный уход (приглашения, право отмечать уход).
- **Сейчас:** нет (#77).
- **Предложение:** `GET /api/v1/sharing` → текущие соухаживающие + статусы;
  `POST /api/v1/sharing/invites` `{plantIds[], inviteeContact, canLogCare}`.
- **Заглушка мобилки:** экран не делаем.

## G23 · Месячный отчёт 🟢
- **Экран:** 14 Месячный отчёт.
- **Закрыто (2026-05-29):** бэк отдаёт `GET /api/v1/reports/monthly?month=YYYY-MM` →
  `MonthlyReportResponse {month, done, overdue, byType{WATERING/MISTING/FERTILIZING/SOIL_CHECK},
  streak, healthTrend[{week (ISO YYYY-Www), done, onTimePct}]}` (проверено curl, `200`,
  user-scoped). Эндпоинт ДОБАВЛЕН в статическую спеку (`resources/reports.yaml`), клиент
  перегенерён (MADR-007). Мобилка: фича `lib/features/report/`, экран 14 (вход — профиль).
  Большие числа (стрик/выполнено/вовремя%/пропуски), разбивка по типам, недельный тренд.
  `onTimePct` общий — взвешенно из `healthTrend` на клиенте (отдельного поля нет).
  Query `month` — строка `YYYY-MM`, НЕ `format: date` (G12-ловушка обойдена).
- **Осталось (новый под-гэп per-plant):** дизайн v4 показывает «Звёзды месяца»
  (медали по растениям) и «Личный рекорд» (самое спокойное растение) — per-plant разбивки
  агрегат `MonthlyReportResponse` НЕ содержит. Эти блоки опущены. Нужно: либо
  `topPlants[{plantId, name, done, ...}]`/`calmestPlantId` в ответе, либо отдельный
  `GET /reports/monthly/plants?month=`. Дельты к прошлому месяцу («+8% к апрелю») тоже нет.

## G24 · Диагноз проблемного растения 🔴
- **Экран:** 15 Диагноз.
- **Сейчас:** нет (#73). Сейчас есть только `GET /plants/{id}/health` (score/zone, G1) —
  без причин/рекомендаций.
- **Предложение:** `GET /api/v1/plants/{id}/diagnosis` → `{issues[], recommendations[]}`
  (на базе истории ухода и health-факторов).
- **Заглушка мобилки:** экран не делаем.

## G25 · AI-доктор 🔴
- **Экран:** 16 AI-доктор.
- **Сейчас:** нет (бэклог, top-1 кандидат).
- **Предложение:** `POST /api/v1/ai/diagnose` `{plantId?, photo?/symptoms[]}` →
  диагноз/рекомендации. Требует продуктового и инфраструктурного решения (модель, фото-пайплайн).
- **Заглушка мобилки:** экран не делаем.

## G26 · Родословная / размножение 🔴
- **Экран:** 18 Родословная (дерево «родитель → отводки»).
- **Нужно:** связь растение↔родитель и обход семьи.
- **Сейчас:** нет (#139). `PlantCreateRequest` принимает `speciesId` (G13a), но не `parentPlantId`.
- **Предложение:** `parentPlantId: int?` в `PlantCreateRequest` + `GET /api/v1/plants/{id}/family`
  → предки/потомки.
- **Заглушка мобилки:** экран не делаем.

## G27 · Список покупок 🔴
- **Экран:** 19 Список покупок.
- **Сейчас:** нет (#136).
- **Предложение:** `GET /api/v1/shopping`, мутации добавления/отметки. Форма — по дизайну экрана 19.
- **Заглушка мобилки:** экран не делаем.

## G28 · Структурный флаг токсичности вида `toxic` 🟡
- **Экран:** 12 Каталог (бейдж 🐈), 20 Карточка вида (баннер токсичности).
- **Нужно:** машиночитаемый признак токсичности вида для **бейджа каталога** (#128) —
  фильтрация/иконка без парсинга текста.
- **Сейчас:** `SpeciesDto` булева/enum-поля `toxic` НЕ содержит. НО `GET /species/{id}`
  отдаёт `facts[]`, где есть факт `category: "TOXICITY"` с человекочитаемым `title/body`
  (источник, напр. ASPCA) — для **баннера на карточке вида 20 этого достаточно**.
- **Закрыто частично (2026-05-29):** баннер токсичности экрана 20 теперь **реальный** —
  рисуется из `facts[category=TOXICITY].body`. Поле `facts` ДОБАВЛЕНО в статическую спеку
  (`resources/species.yaml`, новая `SpeciesFactDto`), клиент перегенерён (MADR-007), domain
  получил `SpeciesFact`/`SpeciesFactCategory` + getter `SpeciesDetail.toxicityFact`. Нет
  TOXICITY-факта → баннер скрыт (без хардкода).
- **Осталось (для бейджа каталога 12):** в `/species` (список, `SpeciesSummaryDto`) фактов
  нет, и парсить текст ради бинарного бейджа нежелательно. Нужен структурный
  `toxic: bool` (или `toxicity: enum`) в `SpeciesSummaryDto`/`SpeciesDto` — тогда оживёт
  бейдж 🐈 в каталоге.
## G16 · Health Score для сетки Home — N+1 🟡
- **Экран:** 01 Home (мини-кольцо здоровья на каждой карточке растения).
- **Нужно:** health (score/zone) для ВСЕХ растений сада одним запросом вместе с сеткой.
- **Сейчас:** есть только per-plant `GET /plants/{id}/health` (G1 🟢). `GET /plants` (список
  сада) health НЕ отдаёт. Кольцо на Home тянет health по одному запросу на растение → N+1.
- **Предложение:** либо `healthScore`/`zone` прямо в `PlantDto` списка (дёшево, один запрос
  на сетку), либо батч `GET /api/v1/plants/health?ids=...`.
- **Заглушка мобилки:** кольцо грузится per-plant через `plantHealthProvider(id)` (Riverpod
  family + autoDispose). Сетка ленивая (SliverGrid) — запросы только для видимых карточек,
  кэш гасит повторы; health не блокирует загрузку растений, на ошибке тихо скрывается.
  Для больших садов (20+) при активном скролле возможна серия мелких запросов — приемлемо
  для MVP, закрыть инлайн-полем/батчем когда бэк добавит.

---

## G29 · Агрегат истории ухода (вовремя % / счётчики по всей истории) 🔴
- **Экран:** 21 Полная история ухода (сводка в шапке: всего / вовремя% / стрик),
  31 Пустой дневник (`total == 0`).
- **Нужно:** агрегат по ВСЕЙ истории ухода растения — `total`, `onTimeCount`
  (или `onTimePercent`), опц. разбивка по типам — независимо от пагинации
  записей. Плюс фильтр истории по типу ухода на стороне сервера.
- **Сейчас:** `GET /plants/{id}/history?limit&offset` отдаёт `{items[], total,
  limit, offset}` — `total` есть, но «вовремя %» считается только из `onTime`
  загруженных `items`. Параметра фильтра по типу (`WATER/SPRAY/FERTILIZE`) у
  эндпоинта нет. `GET /stats/streak` даёт только стрик, не процент.
- **Следствие:** пока загружены не все страницы, «вовремя %» приблизителен
  (по `loadedCount`, не по `total`); клиентский фильтр-чип по типу тоже неполон
  при незавершённой пагинации (фильтрует только подгруженное).
- **Предложение:** `GET /api/v1/plants/{id}/history/summary` →
  `{total, onTimeCount, onTimePercent, byType{WATER,SPRAY,FERTILIZE}}`, либо
  добавить `onTimeCount`/`onTimePercent` прямо в `PlantHistoryResponse`; плюс
  query `type=` для серверной фильтрации истории.
- **Заглушка мобилки:** первичная страница `limit = 50` (для типичного
  растения покрывает всю историю → сводка точная сразу), `loadMore` дотягивает
  активным; `onTimePercent` считается по загруженным записям
  (`CareHistorySummary.loadedCount`), фильтр по типу — клиентский по загруженным
  (`CareHistoryState.visibleEntries`). `TODO(gap #29)` — перейти на серверный
  агрегат/фильтр, когда появится.

---

## Шаблон новой записи
```
## G{N} · {кратко} {🔴|🟡|🟢}
- **Экран:**
- **Нужно:**
- **Сейчас:**
- **Предложение:**
- **Заглушка мобилки:**
```
