# План экранов — карта реализации

> Сводная карта 38 экранов из `design_handoff_plantcare` (дизайн v5–v10, обновлён
> 2026-05-28) с фактическим статусом мобилки и backend-готовностью. Сверено с live-API
> `plants-care-development.up.railway.app` 2026-05-28.
>
> Детальное описание экранов — `design_handoff_plantcare/README.md` §4 и
> `screenshots/INDEX.md`. Backend-дефицит — `docs/BACKEND-GAPS.md`.
>
> **Статус мобилки:** ✅ сделано · 🟢 готово к работе (бэк готов / UI-only) · 🔴 заблокировано бэком.

---

## ✅ Сделано (17 экранов)

| # | Экран | Фича (`lib/features/`) |
|---|---|---|
| 01 | Главная — Мой сад | `home` |
| 02 | Карточка растения | `plant_card` |
| 03 | Сегодня | `home` (today_*) |
| 04 | Мастер добавления (шаги 1–4) | `add_plant` |
| 06 | Sheet'ы ухода (полить/опрыскать/подкормить) | `care_event` |
| 07–09 | Auth (welcome / Telegram-код / welcome back) | `auth` (dev-слот) |
| 10 | Пустой сад | `home` |
| 11 | График недели | `schedule` |
| 12 | Каталог видов | `catalog` |
| 13 | Профиль | `profile` |
| 17 | Архив / Memorial | `archive` (UI-first, fake repo, G15) |
| 20 | Карточка вида | `catalog` (v5: facts/свет/CTA→мастер; баннер токсичности реальный из `facts[TOXICITY]`; бейдж каталога `toxic:bool` — G28) |
| 28 | Загрузка — скелетон | `home` (`HomeLoadingSkeleton`) + `core/widgets` (`SkeletonBox`) — cold-start Home |
| 29 | Офлайн / ошибка связи | `core/widgets` (`OfflineState`) + `home` (`homeViewStateProvider`) — полноэкранный офлайн при `NetworkError` |
| 34 | Дома и места | `rooms` |

---

## 🟢 Готовы к работе — бэк готов / UI-only (5 экранов)

Порядок реализации — сверху вниз (ценность × готовность).

| # | Экран | Опора | Заглушка/нюанс |
|---|---|---|---|
| **21** | Полная история ухода | `GET /plants/{id}/history?limit&offset` ✅ (`onTime`/`performedAt`/`type`) | таймлайн по месяцам, чипы-фильтры, сводка |
| **31** | Пустой дневник | состояние history `total:0` | идёт вместе с 21 |
| **30** | Пустой поиск | `GET /species?q=&offset&limit` ✅ | добавить поиск + empty-state в готовый каталог 12 |
| **33** | Успех первого ухода | после `POST /care-events` ✅ | клиентское празднование (первое событие растения) |
| **38** | Язык приложения | локально (MADR-012, `AppLocalizations`) | синк `PATCH /me {locale}` гапнут (**G16**) — экран локальный |

> **36 Пикер времени** — чистый UI sheet, но обретает смысл только с экраном 23
> (заблокирован G16). Делать вместе с 23, не раньше.

---

## 🔴 Заблокированы бэком (16 экранов)

| # | Экран | Блокер (gap) | Roadmap |
|---|---|---|---|
| 22 | Редактирование расписания | **G19** `GET/PUT /plants/{id}/schedules` (родств. G14) | §12.2 |
| 23 | Тихие часы и время | **G16** `PATCH /me {quietHours, timezone}` | #116 |
| 37 | Выбор таймзоны | **G16** `PATCH /me {timezone}` | #116 |
| 38 | Язык (синк) | **G16** `PATCH /me {locale}` (экран — локально) | #116 |
| 24 | Лента уведомлений | **G17** `GET /notifications`, `POST .../read` | §12.9 |
| 32 | Пустая лента | **G17** (состояние пустого фида) | §12.9 |
| 27 | Онбординг пушей | **G18** `POST /devices` + решение Expo/FCM | §9 #3 |
| 25 | Режим отпуска | **G21** `POST/DELETE /vacation` | #53 |
| 26 | Совместный уход | **G22** `GET /sharing`, `POST /sharing/invites` | #77 |
| 35 | Сезонные интервалы | **G20** сезонные коэффициенты (зависит от G19) | #67 |
| 14 | Месячный отчёт | **G23** `GET /reports/monthly` | #137 |
| 15 | Диагноз | **G24** `GET /plants/{id}/diagnosis` | #73 |
| 16 | AI-доктор | **G25** `POST /ai/diagnose` | бэклог |
| 18 | Родословная | **G26** `parentPlantId` + `GET /plants/{id}/family` | #139 |
| 19 | Список покупок | **G27** `/shopping` | #136 |

---

## Приоритеты backend-гэпов (по разблокированному охвату)

1. **G16 `/me` + `PATCH /me`** — разблокирует 23/37/38 и допиливает профиль 13. Объединяет G5.
2. **G17 уведомления** — разблокирует 24/32 + badge на Home 01.
3. **G19 расписания** — экран 22, заодно закрывает G14 (создание) и подводит к G20.
4. **G28 `toxic`** — мелкое поле, оживляет бейдж каталога 12 и баннер карточки вида 20.

Далее — продуктовый бэклог (крупные фичи, нужны и бэк, и продуктовые решения):
G21 отпуск, G22 шеринг, G23 отчёты, G24 диагноз, G25 AI-доктор, G26 родословная, G27 покупки.

## Открытые вопросы (README §9), блокирующие часть работ
- **Push-стек** (Expo Push vs FCM/APNs) — блокирует G18 / экран 27.
- **Voice lines** (клиент vs бэк `voiceLine`, **G2**) — влияет на тексты карточек уведомлений (экран 24).
