/// Версия каталога SDUI-блоков, которую понимает этот клиент (MADR-015).
///
/// Клиент шлёт её в заголовке `X-UI-Catalog-Version` (`GET /ui/<screen>`), а
/// сервер не отдаёт типы блоков выше неё. Неизвестный `type` клиент всё равно
/// тихо пропускает (forward-compatibility, см. [SduiUnknownBlock]) — версия
/// лишь сужает выдачу сервера до совместимого набора.
///
/// Поднимать ВМЕСТЕ с добавлением поддержки нового типа блока в [BlockRegistry]
/// и domain-маппере.
const int kUiCatalogVersion = 1;
