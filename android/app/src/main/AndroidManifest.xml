<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Нужно для сетевых запросов к API в release-сборке. В debug/profile
         INTERNET добавляется автоматически Flutter-tool'ом, в release — нет. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="PlantCare"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Custom-scheme deep link для magic-link входа (MADR-008):
                 plantcare://auth/verify?token=… Захват URI — через app_links
                 (см. PlantCareApp). Работает без верификации домена. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="plantcare" android:host="auth"/>
            </intent-filter>
            <!-- Universal Links / Android App Links (issue #127):
                 https://plants-care.up.railway.app/auth/verify?token=… и
                 https://plants-care.up.railway.app/plants/:id открывают
                 приложение напрямую (минуя браузер).
                 android:autoVerify="true" — Android при установке проверяет
                 Digital Asset Links на /.well-known/assetlinks.json хоста.

                 Хост-файл требует деплоя на backend'е:
                   GET https://plants-care.up.railway.app/.well-known/assetlinks.json
                   → [{"relation":["delegate_permission/common.handle_all_urls"],
                        "target":{"namespace":"android_app",
                                  "package_name":"com.plantcare.plantcare_mobile",
                                  "sha256_cert_fingerprints":["<RELEASE_SHA256>"]}}]
                 SHA-256 для release-ключа получить через:
                   keytool -list -v -keystore <release.jks> -alias <alias>
                 или из Play Console (App signing → App signing key certificate).
                 Захват URI — через app_links (PlantCareApp._handleDeepLink). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="plants-care.up.railway.app"
                    android:pathPattern="/auth/verify"/>
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="plants-care.up.railway.app"
                    android:pathPattern="/plants/.*"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Android 11+ package visibility: без этих <intent>/<package> запросов
             url_launcher не «видит» Telegram, canLaunch/launchUrl для tg:// и
             https://t.me/… возвращает false и вход через Telegram (экран 08) не
             открывает приложение. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tg"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https" android:host="t.me"/>
        </intent>
        <package android:name="org.telegram.messenger"/>
        <package android:name="org.telegram.messenger.web"/>
    </queries>
</manifest>
